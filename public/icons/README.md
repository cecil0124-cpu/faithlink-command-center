# FaithLink Icons

`faithlink-icon.svg` is the current placeholder PWA icon.

Add production PNG icons later:

- `icon-192.png`
- `icon-512.png`

The manifest currently references the SVG placeholder because PNG generation is deferred until real icon assets are finalized.
